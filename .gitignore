# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# The built database is committed so a fresh clone and the Vercel deployment
# both work without running the pipeline first. Rebuild it any time with
# `python -m app.etl` — the output is deterministic.

# OS / editor
.DS_Store
.idea/
.vscode/
